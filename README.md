# kks/log-enhancer

Enhanced version of Laravel's `info()` logging helper.

This package automatically converts objects, Arrayable, Jsonable, Traversable, etc.
into arrays before logging — preventing errors and improving readability.

## Installation

```bash
composer require kks/log-enhancer
